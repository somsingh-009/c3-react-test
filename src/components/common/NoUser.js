import React from 'react';
import {COMPONENT_CONSTANTS} from "../../constants/Component_Constants";
import "./NoUser.css";

const no_user = ()=> (<p className="no-user">{COMPONENT_CONSTANTS.no_user_found}</p>);

export default no_user;
